import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "inventory.neumasystem.site", "neumasystem.site", "neumasystem.com"],
  },
};

export default nextConfig;
